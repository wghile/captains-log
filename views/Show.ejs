<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🧭 Captain's Log</title>
</head>
<body>
    <h1>
        <%= log.title %>
    </h1>
    <p>
        Log Date: <%= `${log.createdAt.getMonth() + 1}-${log.createdAt.getDate()}-${log.createdAt.getFullYear()}` %>
    </p>
    <p>
        Entry: <%= log.entry %>
    </p>
    <div>
        <!-- Ship Status:  -->
        Ship is Broken?
        <span>
            <% if(log.shipIsBroken == false){ %>
               <!-- <h1>Good Condition</h1> -->
                <input type="checkbox" name="shipIsBroken" />
            <% }else{ %>
               <!-- <h1>Needs Repairs</h1> -->
                <input type="checkbox" name="shipIsBroken" checked />
            <% } %>
        </span>
    </div>
    <a href="/logs">
        Return
    </a>
</body>
</html>